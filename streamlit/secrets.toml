# Copy this file to .streamlit/secrets.toml (locally) or paste its contents
# into your app's "Secrets" panel on Streamlit Community Cloud.
#
# This is the ONE shared password admins use to unlock scoring/input pages.
# Everyone who views the dashboard does NOT need this — only whoever is
# entering scores at the tournament.

admin_password = "olympics123"
